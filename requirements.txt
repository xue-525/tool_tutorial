anthropic>=0.92
gradio>=4.44
jinja2>=3.1
markdown>=3.6
pydantic>=2.7
# Optional — only needed if you want to extract subtitles from video files.
# Also requires `ffmpeg` (apt install ffmpeg / brew install ffmpeg).
# openai-whisper>=20240930
